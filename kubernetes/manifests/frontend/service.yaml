apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: philobiblus
spec:
  selector:
    app: frontend
  ports:
    - name: http
      port: 5173
      targetPort: 5173
  type: ClusterIP