apiVersion: v1
kind: Service
metadata:
  name: {{ include "philobiblus.fullname" . }}-postgres
  labels:
    {{- include "philobiblus.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "philobiblus.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: postgres
  ports:
    - name: postgres
      port: {{ .Values.postgres.service.port }}
      targetPort: postgres
